<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Airbnb Application</title>

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
      crossorigin="anonymous"
    />
  </head>
  <body>
    <div class="container">
      <nav></nav>

      <main class="row">
        <h1>Database CRUD application</h1>
        <div class="col-8">
          <div>
            We can create, read, delete, update District Table and HostResponseTime Table.
          </div>

          <h2>District</h2>
          <ul>
            <% districts.forEach(function (district) { %>
              <li><a href="/districts/<%=district.districtId %>"><%= district.districtId %> 👉 <%= district.districtName %></a>
                <form action="/districts/delete" method="POST">
                  <input type="hidden" name="districtId" value="<%= district.districtId %>">
                  <button type="submit" class="btn btn-danger">Delete</button>
                </form>
              </li>
            <% })%>
          </ul>
          <h2>HostResponseTime</h2>
          <ul>
            <% hostresponsetimes.forEach(function (hostResponseTime) { %>
            <li><a href="/hostresponsetimes/<%=hostResponseTime.hostResponseTimeId %>"><%= hostResponseTime.hostResponseTimeId %> 👉 <%= hostResponseTime.hostResponseTime %></a>
              <form action="/hostresponsetimes/delete" method="POST">
                <input type="hidden" name="hostResponseTimeId" value="<%= hostResponseTime.hostResponseTimeId %>">
                <button type="submit" class="btn btn-danger">Delete</button>
              </form>
            </li>
            <% })%>
          </ul>
        </div>
        <!-- /.col-8 -->

        <div class="col-4">
          <h2>Create District</h2>
          <form action="/districts/create" method="POST">
            <div>
              <label class="form-label"
                >New District Name:
                <input
                  required="true"
                  name="districtName"
                  type="text"
                  class="form-control"
              /></label>
            </div>

            <button type="submit" class="btn btn-primary">Create</button>
          </form>

          <h2>Create Host Response Time</h2>
          <form action="/hostresponsetimes/create" method="POST">
            <div>
              <label class="form-label"
              >New Host Response Time:
                <input
                        required="true"
                        name="hostResponseTime"
                        type="text"
                        class="form-control"
                /></label>
            </div>

            <button type="submit" class="btn btn-primary">Create</button>
          </form>
        </div>

        <!-- /.col-4 -->
      </main>

      <footer>
        <div class="row">
          <div class="col-4">About</div>
          <div class="col-4">Made By Zejun Liu</div>
          <div class="col-4">Copyleft ©</div>
        </div>
      </footer>
    </div>
  </body>
</html>
