{
  "name": "p2",
  "version": "1.0.0",
  "description": "1.to create db and collection in mongodb, you can either run: mongoimport --db p2 --collection city < city.json",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "mongodb": "^4.2.0"
  }
}
